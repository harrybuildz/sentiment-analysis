# sentiment-analysis
Claude-powered artifact that takes a topic/product name, fetches recent Reddit posts via the Reddit JSON API (no auth needed for basic reads), and scores sentiment with custom phrase weights.
